# TypeScript Commodore 64 Emulator 工程规范

本文件适用于仓库根目录及其全部子目录。子目录如需更严格的局部约束，可以增加自己的 `AGENTS.md`，但不得削弱本文件中的类型安全、硬件行为、测试和代码质量要求。

## 1. 项目定位

- 项目全名为 **TypeScript Commodore 64 Emulator**，npm 包名为 `typescript-commodore-64-emulator`。
- 项目使用严格 TypeScript 现代化复刻 Commodore 64。硬件外部可观察行为必须与目标机型一致，内部实现可以使用现代算法、数据结构和工程方法。
- CPU 运算结果与总线访问顺序、VIC-II/CIA/SID 时序、PLA 映射、IRQ/NMI、IEC、磁带和磁盘设备交互属于必须复刻的行为边界。
- React、Canvas、Web Audio、事件分发、缓冲区、重采样、序列化、测试工具和 UI 状态管理不要求复刻旧计算机内部实现，但不得改变硬件层可观察行为。
- 第三方模拟器和测试程序只能作为独立行为参考或测试预言机，不能替代项目自己的硬件实现。

## 2. 文件头与模块说明

文件头说明模块身份和维护归属。所有新建的 TypeScript、TSX、JavaScript、Python 以及其它支持注释的源码和测试文件必须使用统一文件头。JSON、二进制参考程序、外部工具生成物以及不支持同类注释的格式遵从其原生格式。

TypeScript、TSX 和 JavaScript 使用以下格式：

```text
// +-------------------------------------------------------------------------
//
//   TypeScript Commodore 64 模拟器 - 模块中文名称
//
//   文件:       FileName.ts
//
//   日期:       YYYY年MM月DD日
//   作者:       Author Name
// --------------------------------------------------------------------------
```

Python 等使用 `#` 注释前缀的文件采用相同内容和布局，仅替换注释前缀。

- “模块中文名称”必须描述文件的单一主要职责，例如“VIC-II 周期时序器”“CPU 中断采样时序”或“运行控制面板”，不得使用“工具”“公共模块”等空泛名称。
- “文件”必须与当前文件的实际 basename 完全一致，并采用本项目规定的现代 PascalCase 或既定配置文件命名。
- “日期”记录文件创建、重命名或实质重构日期，不得省略。格式固定为 `YYYY年MM月DD日`。
- “作者”填写实际创建或执行本次实质重构的主体。由 Codex 创建或实质重构时填写 `OpenAI Codex`，不得借用用户或历史作者姓名。
- 文件重命名或实质重构时，必须同步更新文件名、日期和作者。普通小修不改日期和作者。
- 不得为了给无关旧文件补头制造大面积 diff；只处理本次新建、重命名或实质重构的文件。
- 文件头不代替职责设计、类型约束、来源说明和测试。边界混乱的文件即使有完整文件头仍不合格。
- 上游版权和许可证原文必须原样保留。项目文件头不是法律声明，不得覆盖或伪造第三方作者信息。
- 项目自有源码与测试文件的文件头禁止添加 `SPDX-License-Identifier` 或其它许可证标识。项目许可证只在许可证文件、包元数据和第三方声明中维护。
- 第三方来源、固定版本、校验值与许可证关系集中记录在第三方声明或参考验证清单中；实现注释解释本项目的职责、硬件原因、时序、单位和不变量，不把 `GPL-2.0`、`MIT` 等许可证标识混入算法说明。只有直接保留上游源码或数据所附的法律声明时例外。

## 3. TypeScript 与 React

- TypeScript 检查必须保持严格，不得通过 `any`、双重类型断言、`@ts-ignore`、关闭编译选项或伪造非空值绕过类型系统。
- 仅含类型、算法、设备模型、状态、服务或非 JSX UI 逻辑的文件使用 `.ts`。
- 包含 JSX 的 React 组件使用 `.tsx`。不得在 `.ts` 中放 JSX，也不得把不含 JSX 的硬件或业务逻辑放进 `.tsx`。
- React 组件只负责界面组合和用户交互。CPU、内存、芯片、媒体解析和机器调度不得依赖 React。
- 公共接口优先使用明确的只读类型、判别联合和依赖接口。类型名称、单位和生命周期必须能从声明中读懂。

## 4. 命名与常量

- TypeScript 类、组件和主要模块文件使用现代 PascalCase，例如 `TypedEventEmitter.ts`、`VicCycleSequencer.ts` 和 `ControlPanel.tsx`。
- hooks 和普通函数模块可以使用语义明确的 camelCase，例如 `useC64Emulator.ts` 和 `basicStartup.ts`。
- 禁止使用历史遗留式、含义不明或玩笑式文件名，不使用 `kingsquare` 一类命名。
- 硬件地址、位掩码、时钟周期、容量、偏移、行列数和协议值必须放在有语义的常量或型号配置中。测试中的输入值如果本身就是被验证的规范值，也应由测试名称或局部常量解释。
- 常量名称必须包含必要的设备、单位或语义，例如 `PAL_CPU_CLOCK_HZ`、`VIC_RASTER_LINE_COUNT`，不得留下无法判断含义的裸数字。
- 不为消除数字字面量而机械制造无意义常量；循环步长 `1`、二进制换算等上下文完全明确的值可以直接使用。

## 5. 架构与解耦

- 依赖方向固定为：`app -> video/platform -> core -> devices/media/shared`。底层硬件模型不得反向依赖 React、DOM、Canvas 或浏览器 API。
- CPU、VIC-II、SID、CIA、PLA、IEC 和外设通过窄接口、总线事务或明确事件协作，不得互相读取对方的私有状态。
- 机器调度器负责时钟推进和总线仲裁；设备负责自身状态机；渲染器只消费已经锁存的视频状态；音频输出只消费 SID 产生的样本。
- 区分硬件状态、派生展示状态和 UI 状态，禁止用 UI 所需字段反向驱动硬件真值。
- 优先组合而非继承。事件订阅必须可解除，资源生命周期必须有明确所有者。
- 不加入只为某个程序、ROM、测试或演示通过的地址特判、时序补丁或静默兼容分支。
- 不使用掩盖错误的 fallback。外部资源缺失、哈希不符、非法媒体、总线周期不变量破坏和不支持的硬件模式必须产生明确错误。

## 6. 硬件复刻与现代算法

- 先定义目标芯片型号和视频制式，再实现其行为；不同芯片修订版通过显式配置建模，不能混合成隐式“兼容模式”。
- 模拟精度以“目标软件、媒体或用户输出能否观察到”为边界。寄存器结果、中断边沿、总线访问、DMA 争用、引脚耦合、光栅时序和媒体位流等可观察语义必须复刻；不能观察到的内部组织可以采用更清晰、高效且可测试的现代实现。
- 模拟电阻公差、随机噪声、机械惯性、显像管或元件老化等模拟细节前，必须先说明目标型号、可观察影响和独立验证依据。没有兼容性证据时不得为了“更像硬件”向确定性核心加入随机扰动。
- 可以明确提供高层便捷能力，例如即时装载、现代显示缩放和浏览器输入映射，但它们必须位于平台或应用层，不得悄悄改变硬件寄存器、周期顺序或总线状态。
- 精度取舍必须在最接近实现的位置记录：说明保留了哪些可观察行为、主动省略了哪些不可观察细节，以及何种测试证据会触发进一步细化。
- 周期精确模块必须使用命名阶段、状态机和总线事务表达，不能用每帧扣除若干周期的粗略预算替代。
- 可以使用稳定开源库实现通用数据结构、属性测试、环形缓冲、音频重采样、压缩或解析，但引入前必须评估维护状态、许可证、包体积和浏览器兼容性。
- 不为简单问题引入大型依赖。第三方依赖不得隐藏关键硬件状态或让精确时序不可测试。
- 来自 VICE、Klaus Dormann 等项目的测试程序必须固定来源、版本或 SHA-256；下载内容在执行前必须校验，测试失败不得自动降级为项目自有断言。

## 7. 测试与质量门禁

- 新硬件行为至少需要一条项目内确定性测试；具备成熟外部测试时，还要增加独立参考验证。
- 修复时先添加能够重现问题的测试，再修改实现。不得削弱断言或扩大容差来隐藏偏差。
- CPU 指令声明周期与实际总线访问数、视频帧周期、媒体边界和缓冲容量等关键不变量必须显式检查。
- UI 改动需要桌面和移动视口验证，并检查浏览器控制台。硬件改动需要回归现有 CPU、CIA、VIC-II 参考程序。
- 完成相关修改前至少运行：

```text
npm run typecheck
npm run lint
npm run test
npm run build
```

- 涉及硬件时再运行 `npm run verify:reference`；涉及前端时再运行 `npm run verify:browser`。
- 不得宣称某个芯片或整机“完整复刻”，除非相应官方功能、边界时序、媒体路径和独立行为基准均已覆盖并通过。

## 8. 修改纪律

- 使用小而聚焦的变更，保留用户已有修改，不清理与当前任务无关的文件。
- 源码修改后保持 Prettier 和 ESLint 通过。格式化可以机械执行，但不得顺带重排无关代码。
- 删除旧实现前确认所有调用方已经迁移；不得保留两个相互竞争的实现并以隐式 fallback 选择。
- 项目自有源码、测试和工具中的维护性注释默认使用简体中文。芯片型号、寄存器名、信号名、指令名、总线阶段（如 `BA`、`AEC`、`φ1`、`φ2`）和无法准确翻译的标准术语保留官方写法。
- 注释解释硬件原因、时序依据、数据单位、边界条件、不变量或非显然决策，不逐行翻译代码，也不复述类型和函数名已经表达的表面行为。
- 复杂状态机、半周期总线顺序、芯片修订版差异、ROM/PLA 映射和外部参考算法必须在最接近实现的位置写清中文依据。
- 上游版权与许可证原文、外部测试程序内的原始注释不得擅自翻译或改写；必要时在其后补充独立的中文说明。
- TODO 必须使用中文写清缺失的具体硬件行为、已知影响和验收条件，不使用“以后优化”“临时处理”等模糊措辞。
